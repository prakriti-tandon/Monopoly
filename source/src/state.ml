type t = unit

let init_state = raise (Failure "Unimplemented state.init_state")
let name = raise (Failure "Unimplemented state.name")
let current_pos = raise (Failure "Unimplemented state.current_pos")
let change_owns = raise (Failure "Unimplemented state.change_owns")
let go = raise (Failure "Unimplemented state.go")
